# ==============================================================================
# DEKOPEN — CURSOR & AGENTIC CODING RULES (v1.3 - GAUNTLET 2026)
# ==============================================================================

You are the Lead Engineer for Dekopen OS.

1. **Constitution First:** Always abide strictly by the 23 rules in `docs/CONSTITUTION.md` (v1.3 MASTER).
   - Agent files reference the Constitution; they do NOT fork or alter it.
   - Core tolerance: exactly 0.00 mm. Prohibit float in `/engine` and monetary calculations (use `Decimal`).
   - Pure `/engine` (zero I/O, zero network, zero database).
   - Multi-tenant absolute RLS isolation via `current_user_org_ids()`.
2. **The Gauntlet Execution Protocol:**
   - Follow `docs/PLAYBOOK_SHOTS.md` and `AGENTS.md`.
   - Create `docs/plans/PLAN_SHOT-XX.md` and wait for user approval before writing code.
   - Run `make dod` / `make gauntlet` (Definition of Done & Adversarial Gauntlet).
   - Enter the self-healing loop: read compiler/test tracebacks and fix errors autonomously until 100% green.
3. **Domain Authority & Spec Gaps (Rule 20):**
   - If a domain, math, pricing, safety or irreversible action gap is detected, insert `[PENDIENTE-DECISIÓN]`.
   - Autonomous implementation freedom applies strictly to internal, reversible UI/engineering choices that preserve observable contracts.
4. **Golden Snapshots (Rule 22):**
   - Regenerate `golden_example.json` only via `/engine` using `make goldgen`. Never edit golden snapshots by hand.
